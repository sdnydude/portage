# Docs Edit Pass Log — 2026-07-17 (prompt 002, EDIT + GRAPHICS)

Source worklist: `docs/audits/2026-07-docs-audit-worklist.md`. Open questions (§9 of the master report) were UNRESOLVED at execution time — every line whose correct outcome depends on one is skipped with its question number. NEW-group lines (N1–N6) belong to prompt 003 and are not in scope here.

Team: 7 writer agents (docs-writer, Fable) + 3 editor agents (general-purpose, Fable) + orchestrator. Editor slices: **Editor A** = website/docs/api (13 files) · **Editor B** = architecture/frontend/design/development/reference/team-process/appendix (13 files) · **Editor C** = top-level pages + repo working docs + README/ONBOARDING/.env.example + _category_.json (20+ files). Every changed file was reviewed and signed off by exactly one editor; writers did not self-approve.

## EDIT

| Line | Status | Notes (files touched · verification · editor) |
|---|---|---|
| E1 | **SKIPPED** | Blocked on open question **Q3** (nav wording is woven through the mandated rewrite). Credentials block removed anyway under E2; features link + ONBOARDING link fixed under E30. README otherwise untouched. |
| E2 | **DONE** | Demo credentials removed from README.md, ONBOARDING.md, docs/TODO.md:419 → each now says credentials live in Doppler. Repo-wide md sweep found remaining copies in `.claude/skills/frontend-verification/SKILL.md` — **out of docs-pass scope, flagged to Stephen** (config files were off-limits this pass). Editor C credential sweep: clean. |
| E3 | **DONE** | website/docs/api/items.md rewritten against apps/api/src/routes/items.ts + db/schema.ts (field names, list/comps shapes, bulk constraints, syncWarnings, 4 added endpoints incl. token-auth photo export). Editor A signed off. |
| E4 | **SKIPPED** | Blocked on **Q3** (5-tab shell is the substance of the post-R0 rewrite). |
| E5 | **DONE** | website/docs/competitive-analysis.md — 2 live marketplaces, rembg billing-gated, Gemini 2.5 primary (vision.ts verified), carrier row deleted (PR #142), strategic priorities refreshed, differentiators table → features link, point-in-time banner. Editor C signed off (Free-tier claim verified against `SUBSCRIPTION_TIERS`). |
| E6 | **SKIPPED** | Blocked on **Q1** (51/52 + test-count reconcile) and **Q3** (nav wording). CLAUDE.md files left untouched — note they carry pre-existing uncommitted modifications from another session. |
| E7 | **SKIPPED** | Blocked on **Q3** (nav line) and on G1 (also Q3-blocked). E30's Deep-dives links were still added (see E30) — content edits deferred. |
| E8 | **DONE** | website/docs/architecture/database.md — items/listings/notifications columns corrected per schema.ts; aspects + serialized ebaySku stories + inert ebayOfferId line added. Editor B verified every column claim against schema.ts. |
| E9 | **DONE** | website/docs/features.md — photo cap 24, metrics 8 (metrics.ts verified), Reverb OAuth → PAT-live, 4 new feature rows (PRs #231, #220/#224, #223–226, #229 verified via gh), date refreshed; "Only in Portage" structure preserved. Editor C signed off. |
| E10 | **DONE** | website/docs/api/error-handling.md — MARKETPLACE_ERROR/SERVICE_UNAVAILABLE deleted; MARKETPLACE_UNAVAILABLE 503, PORTER_LIMIT_REACHED, USER_NOT_FOUND dual-status note, and the full missing-code list added, each grep-verified to its call site. Editor A signed off; flags logged: table intentionally non-exhaustive (7 endpoint-page-only codes), 401-row wording wart, 403-under-Rate-Limiting placement. |
| E11 | **DONE** | All 11 api files re-verified against their route handlers: overview (base URL https://10.0.0.251:8016 — TLS on 8016 verified live by orchestrator; endpoint groups; /backend reconcile), authentication (preference keys, GET /users/me/preferences, session fields), billing (limits, 10/hr, STRIPE_PORTAL_CONFIG), porter (response shape, 500/day Pro, tool params), scan ({identification,detailed}, 201), images (rembg, cap 24, r2 path route, error codes), listings (publishMode/suppress7d, ebay-offer route), orders (pagination/join fields; :64 login-sync claim reworded to code-verified behavior), drafts ({cleaned:true}), marketplace (encrypted token field names), admin (maintenance_mode allowlist, reset-usage scope, Porter-config mention resolved per code). Editor A signed off all; one editor fix in scan.md (error-table header order). |
| E12 | **DONE** | website/docs/deployment.md — services 4→5, portage-graph :8018 row (compose-verified, no healthcheck), monitoring health cross-link. Editor C verified 4-of-5 healthchecks claim against docker-compose.yml. |
| E13 | **DONE** | .env.example — EASYPOST_API_KEY + Etsy header removed; EBAY_PROD_CLIENT_ID/SECRET, REVERB_API_TOKEN, METRICS_SECRET added (usage grep-verified in apps/api), placeholders only. environment-variables.md — API_PORT/WEB_PORT/NODE_ENV rows. Editor C: placeholders confirmed; **pre-existing flags** (not touched): duplicate APP_URL at :24/:99 (dotenv shadowing footgun), local-dev DATABASE_URL default. |
| E14 | **DONE** | website/docs/frontend/scan-flow.md — cap 24 (constants.ts:60 verified), camera zoom + Continuity section (camera-capture.tsx + PRs #220/#224), 5 component rows (all files verified to exist; scan-fab unreferenced status grep-proven), differentiation framing. Editor B signed off. |
| E15 | **SKIPPED** | Blocked on **Q7** (style-guide's DHG-wide home) — and its TabBar fix touches the Q3-frozen nav wording. Only the E32 frontmatter title fix was applied to style-guide.md. |
| E16 | **DONE** | website/scripts/gen_sitemaps.py — added /tutorials, /tutorials/[topic] (TUTORIALS cluster in both orientations; entries verified: more/page.tsx:146, home/page.tsx:466, settings/help:47) and /inventory/[id]/preview (Share Preview card; inventory/[id]/page.tsx:492); headers 35→38 (find page.tsx = 38); admin label 10→11 (11 admin page.tsx). sitemap.md counts updated (:9, :35). Collision checker: 0 collisions on all three variants. Editor B + Editor C verified counts independently. |
| E17 | **DONE** | website/docs/getting-started.md — test count dropped (→ TODO.md pointer), Doppler/non-Doppler env paths split, protocol claims made internally consistent, Next Steps extended, 3-bullet differentiation block, api/images link at rembg mention. Editor C signed off (anchors verified). |
| E18 | **DONE** | memory-system.md → 9 briefing sections per .claude/hooks/session-briefing.sh (read); registry-integration.md → post-session-reports pipeline added, 9 subcommands matched 1:1 to memreg_capture.py; code-graph.md → dated count (716 wiki articles, counted on disk). Editor B re-verified all three. |
| E19 | **DONE** | development/history.md — Current Status → snapshot-as-of-2026-07-09 banner + TODO.md pointer; missing PR #51 row added (gh-verified); PORTAGE_HISTORY.md cross-links both directions. Editor B signed off. |
| E20 | **DONE** | reference/ebay-ato-and-publish-hardening.md — top `:::warning` banner: publish-path sections superseded by Trade-First (PR #133, 2026-06-30); ATO + SKU/UA hardening remain current. **Deviation logged:** banner links architecture/marketplace-adapters instead of N1 (N1 is a prompt-003 page; relink when it exists). Editor B signed off. |
| E21 | **DONE** | team-process/outcomes-study.md — marked COMPLETE, Outcome section (109 ship sessions, verified live against registry /api/ship-sessions pagination) vs the 5-run pilot; generator artifact line deleted. Kept as its own page (lighter-touch option; merge-into-history not required by verdict). Editor B signed off. |
| E22 | **DONE** | architecture/marketplace-adapters.md — Reverb OAuth → obsolete (PAT live-proven, PRs #173–177); MarketplaceListingInput/Result contract + mpn-vs-model gotcha (ebay-adapter.ts:421–423 "Does Not Apply" sentinel verified); why-Trade-First-matters paragraph. Editor B signed off. |
| E23 | **DONE** | ONBOARDING.md — Etsy dropped, hard counts → TODO.md pointers, auth → CF Access/JWKS/15m JWT (cf-access.ts + jwt.ts verified), getting-started link added (Editor C fixed to trailing-slash URL form — see infra flag), unique content untouched. Editor C signed off. |
| E24 | **SKIPPED** | Blocked on **Q1** (header task count is ⚠ASK; the refresh reconciles around it). Credentials line :419 was replaced under E2 (unconditional). |
| E25 | **DONE** | docs/ebay-api-reference.md — Current Integration rewritten to Trading-API lifecycle (client + builders files verified to exist); REST tables kept with "scraped 2026-05-07" scope note. Editor C signed off. |
| E26 | **DONE** | Banners: trade-first-burndown EPIC COMPLETE 2026-07-02 (date verified via commit e22ca89; banner's "still open at close" contradiction caught by Editor C, fixed by orchestrator — final item 2.5 verified fixed 2026-07-02) · PORTAGE_HISTORY §7 snapshot banner + §6 voice-container note · ADMIN_PLAN IMPLEMENTED (PR #188 gh-verified) · incomplete-work-backlog SUPERSEDED stamp (kept, not deleted — minimum option) · both superpowers plans EXECUTED (PR #231 / PR #229), matching the listing-hub plan format. Editor C signed off. |
| E27 | **DONE** | docs/secrets-guide.md → pointer + preserved 4-command quickstart (unique content retained per writer report); environment-variables.md Doppler section trimmed to quickstart + link; deployment.md keeps one line + link. Editor C signed off (doppler URL curl-checked 200). |
| E28 | **PARTIAL** | Done: TO-DOS.md, findings.md, progress.md, task_plan.md git-rm'd · docs/voice-chat-mockups/ (10 tracked files) git-rm'd (tag voice-parked-2026-07 preserves) · `docs/eba-ad cloudfllare-dev-docs/` + `docs/scrrenshots/` removed (verified AppleDouble/.DS_Store litter only, zero tracked files) · 47 .DS_Store/._* working-tree litter files removed · .gitignore already contains `.DS_Store` + `._*` (lines 8–9), no edit needed. Zero inbound links to any deleted file (grep-verified) — no pointer stubs required. **Skipped sub-item:** ship-log 039–042 orphan deletions — blocked on **Q2** (generator stale-file cleanup covers them if the pipeline is revived). |
| E29 | **PARTIAL** | Done: unique top-level sidebar_position 1–6 (getting-started/features/competitive-analysis/environment-variables/deployment/monitoring); positions added to competitive-analysis, development/code-graph (5), appendix/screenshots (1); 8 `_category_.json` created (api, architecture, design, development, frontend, reference, team-process, ship-log — ship-log position 98 collapsed:true). Autogenerated sidebar → no entries can point at removed files (requirement 6 satisfied by construction; build link-check green). **Skipped sub-items:** Product grouping + moving reference/ebay-ato — ⚠ASK per worklist (URL changes). Editor C validated JSON + uniqueness. |
| E30 | **DONE** | Cross-links: architecture/overview → Deep dives (4 sub-pages) · getting-started Next Steps (via E17) · deployment ↔ monitoring (via E27/E12 writer) · marketplace-adapters → ebay-ato reference · README → features (URL fixed to live http://10.0.0.251:8017/portage/features/, LAN-only noted) + ONBOARDING.md link · history.md ↔ PORTAGE_HISTORY.md (via E19). Editors A/B/C verified all link targets exist. |
| E31 | **SKIPPED** | Blocked on **Q2** (revive vs retire decides whether fixes go in the generator or the index). Ship-log untouched. |
| E32 | **DONE** | style-guide.md frontmatter title → "DHG Design System v1" (matches H1); `slug:` → `id:` on features.md + competitive-analysis.md (URLs unchanged — verified in build output); id+title-matches-H1 spot-checked across touched pages by all three editors. |
| E33 | **SKIPPED** | Blocked on **Q9** (absolute-/docs/-link convention is load-bearing for the deploy sed rewrite; migration needs the decision). New links added this pass follow the existing convention. |
| E34 | **DONE** | Commit `aebf33c` "docs: add untracked session reports + plan (audit E34)" — 4 session reports + 2026-07-14 onboarding-expansion plan (5 files, 2151 insertions). Edit-pass changes themselves left uncommitted for review. |
| E35 | **DONE** | docs/README.md created — status-column index (LIVE/SNAPSHOT/ARCHIVED/HISTORICAL + audits/), entries verified against ls. Editor C: labels consistent with the E26 banners. |
| E36 | **PARTIAL** | "Last verified: 2026-07-17" stamps on features.md (refreshed), competitive-analysis.md, getting-started.md, deployment.md. **Skipped:** app-structure.md stamp — its parent line E4 is Q3-blocked; stamping an unverified page would be false. |

## GRAPHICS

| Line | Status | Notes |
|---|---|---|
| G1 | **SKIPPED** | Blocked on **Q3** — the 5-tab + Scan nav is a mandated element of both recreated diagrams. architecture-*.svg untouched. |
| G2 | **DONE** | All 8 ebay-trade-first SVGs wired into marketplace-adapters.md via ThemedImage (light+dark pairs; JSX paths use /portage/img/ per the deploy-pipeline convention); phases.svg captioned as the dated 2026-06-27 rollout record; frozen-Inventory panel captioned migration-era. Editor B validated the MDX; orchestrator confirmed all 8 SVGs render in the built HTML. |
| G3 | **SKIPPED** | Blocked on **Q12** (sync-phases contradiction) and **Q6** (canonical side of the byte-identical twins). memory-*.svg untouched. |
| G4 | **DONE** | Regenerated after E16: 3 SVGs (collision checker 0/0/0) + portage-sitemap.pdf re-rendered (headless Chrome, single-page A3 landscape, parity with old PDF). |
| G5 | **DONE** | website/static/img/docusaurus.png deleted — unreferenced (only the audit files mention it); was untracked (covered by the *.png gitignore rule). |
| G6 | **SKIPPED** | Blocked on **Q5** (canonical side confirmation before deleting the docs/ copy). |
| G7 | **SKIPPED** | Blocked on **Q4** (disposition of the 76 unlinked verification PNGs). |
| G8 | **SKIPPED** | Out of scope for prompt 002 — its own constraint says no new diagrams (belongs to prompt 003 alongside N1–N6). |
| G9 | **SKIPPED** | Deferred by the worklist itself — waits for R1 (PR #237) to merge. |

## Verification

- **Build:** replicated `.github/workflows/deploy-docs.yml` exactly in a scratchpad mirror of `/home/swebber64/DHG/aifactory3.5/dhgaifactory3.5/docs-site` (copy website/docs → projects/portage, both sed rewrites, copy static img, `npm run build` with existing node_modules hardlinked): **SUCCESS, exit 0**, with `onBrokenLinks: 'throw'` in force and zero broken-markdown-link/image warnings. Not run: `npm install` step and the live-site build/restart/ingest steps (deliberately — no push happened; the live dhg-docs site was not touched).
- **Stale-marker sweep:** zero hits for /porter/transcribe, /porter/speak, dhg-stt, dhg-tts, etsy auth routes, and listings/[id]-as-edit-surface outside historical records (ship-log entries, docs/TODO.md completed-removal ledger, banner-stamped PORTAGE_HISTORY snapshot; listings/[id] appears only as documented redirect).
- **Editor sign-off:** Editor A — 13 api files (1 fix: scan.md table header). Editor B — 13 architecture/frontend/design/development/reference/team-process/appendix files (0 fixes needed). Editor C — top-level + working docs + config files (2 fixes: trailing-slash URLs in README/ONBOARDING). Every changed file signed off.

## Flags for Stephen (found during the pass, not actioned)

1. Demo credentials also live in `.claude/skills/frontend-verification/SKILL.md` — outside docs scope; needs a config-scope removal.
2. dhg-docs nginx drops the `:8017` port on slash-less-URL redirects (`absolute_redirect` candidate) — will break any future slash-less deep link.
3. `.env.example` pre-existing: `APP_URL` defined twice (:24, :99) with different values; local-dev `DATABASE_URL` embeds the default portage:portage credential.
4. error-handling.md table is intentionally non-exhaustive — 7 endpoint-page-only codes exist (MISSING_KEY, MISSING_TOKEN, PHOTO_FETCH_FAILED, INVALID_QUERY, INVALID_KEY, INVALID_INPUT/NO_FILE/INVALID_FILE_TYPE); decide if it should be.
5. E20's banner links marketplace-adapters as a stand-in for the not-yet-written N1 page — relink when prompt 003 creates it.
6. Worklist execution note (`graphify update .` + registry re-ingest) applies after this pass lands on main — deploy-docs.yml handles the site on push to website/**.

**Totals:** EDIT 25 done + 3 partial + 8 skipped (of 36) · GRAPHICS 3 done + 6 skipped (of 9) · 79 paths changed in the working tree (+1 commit for E34).

---

# Phase 1 + 2 — Findings fix pass + Q-verdict cleanup (2026-07-17, docs-refresh landing)

Executed from `docs/audits/2026-07-tree-review-findings.md` (Phase 1) and the approved landing plan's Q-verdicts (Phase 2). Team: 5 parallel writer agents (Fable) + orchestrator + 2 editor agents (Fable). Every fix verified against the cited code at edit time; every changed file second-read by an editor (writers never self-approved). Per-finding dispositions are recorded in the findings report itself (Disposition column / inline stamps); this section is the execution record.

## Phase 1 — findings

| Severity | Count | Outcome |
|---|---|---|
| HIGH (1–12) | 12 | all **FIXED** — README ×8 (W1), style-guide ×2 + overview + app-structure (W2), root CLAUDE.md copy-source (W1) |
| MEDIUM (13–23) | 11 | all **FIXED** — api docs (W3), scan-flow/code-graph/ebay-ato/ai-pipeline+features/burndown (W4), TODO.md (orchestrator), infra-secrets-path SVG pair (orchestrator: `REGISTRY_URL` → `STRIPE_*`, label parity re-verified byte-equal) |
| LOW (16 items) | 16 | all **FIXED** — see the findings report's LOW disposition block for deviations (WEB_PORT consumed-nowhere; ScanFab row deleted not corrected; ADMIN_PLAN via widened banner) |
| Side findings | 2 docs **FIXED** (rembg 7000; VISION_PROVIDERS → ai-client.ts) · 3 code items **NOT TOUCHED** per scope (porter.ts phantom tool, @imgly dep, EASYPOST key — already registered deferred) |
| Deletion residuals | (a) 6 voice-mockup PNGs **DELETED** (reference-grep clean) · (b) 4 ship-log orphans **ACCEPTED** (Q2 workstream) |

## Phase 2 — Q-verdict cleanup

- **Q3 (5-tab + Scan, one canonical phrase):** DONE — canonical phrasing "5 tabs (Home, Inventory, Listings, Porter, Orders) + center Scan button; More via avatar menu" now in README, root CLAUDE.md, apps/web/CLAUDE.md, architecture/overview, frontend/app-structure (full E4 post-R0 rewrite: AppShell/Sidebar/TopBar, R0 shell files, new routes, HIG cite, link to responsive-shell), design/style-guide (E15 scope note added — stays in Portage per Q7). **G1 DONE**: both architecture-*.svg fully recreated in place (same filenames; CF Access auth, 5 compose services + external dhg-docs/Registry, 18 tables, Trade-First + Reverb PAT, vision chain, R0 shell nav, carrier/bcrypt/Etsy/localStorage content gone; every label code-verified by writer + editor).
- **Q1 (counts):** suites run this session — **API 686/686, web 467/467, both green**; stamped into CLAUDE.md Progress and docs/TODO.md header with 2026-07-17 date. **Roadmap recount arithmetic:** `grep -c '^- \[.\] \*\*Task'` = **49** task lines; distinct task numbers = 48 (**#53 used twice**; #22, #26, #28–#31 appear nowhere — `git log -S` shows the ledger began life as "31/46 tasks", the 52 denominator was never reproducible from this file); `[x]` = **48**, `[~]` = 1 (Task 21 superseded). Header reconciled to **48/49 + 1 superseded** in both TODO.md and CLAUDE.md, with the recount note inline; Phase-4 subheader corrected 6/7 → 7/7; independently re-derived by Editor B.
- **Q4/G7 (verification PNGs):** **90 files moved** to `docs/verification-archive/` (17 subdirs, structure preserved, git-mv for tracked): the 76 unlinked sets **+ 14 docs-refresh proofs** (reviewer flag). **10 files stayed** — `inline-edit/` (4) + `photo-tools/` (6), referenced by `website/docs/development/frontend-e2e-verification.md` (grep-proven). TODO.md's two prose path-mentions (sold-list, voice-removal) repointed to the archive. Deployed build output now contains only the two referenced sets.
- **Q5 (docs/screenshots):** **DELETED** — all 117 files `cmp`-verified byte-identical to canonical `website/static/img/screenshots/`; zero non-audit references.
- **Q6 + G3/G6 (memory SVGs):** 4 twins in `docs/superpowers/architecture/` byte-identity-verified then **DELETED**; `memory-intelligence-narrative.md` repointed to the canonical `website/static/img/memory-*.svg`. Counts refreshed on website copies: briefing 7→**9** sections, 23→**124** memory files (22 decision / 36 feedback / 32 project / 25 reference + patterns/index), 37/52→**48/49**, 54→**76** Doppler secrets (CLI count). **Q12 resolved from ground truth:** light mode is defined in `.claude/commands/sync-memory.md` but invoked nowhere (cron runs full daily 6:30am; no hook passes "light") — sync-phases SVG now says "defined but currently unwired" / "not wired (hook removed)", contradiction gone. Editor B caught + fixed two enumeration mismatches (section lists vs the new 9-count) against session-briefing.sh.
- **Q11 (session_reports):** verdict adjusted to verified reality — live POST to `/api/kb/search` **rejects** `sources:["session_reports"]` (Invalid sources). Both `website/docs/development/registry-integration.md` and `.claude/rules/registry-search.md` now document session_reports as a capture pipeline that is **not yet a KB-search source** (dated, live-verified twice). Registry-side gap posted as a deferred item.
- **Q9/E33:** the absolute `/docs/` + `/img/` convention documented in deployment.md (sed rewrites quoted verbatim from deploy-docs.yml:26,30); E33's link migration is thereby **closed as won't-do** — the convention is deliberate and now written down.
- **Flag 5 closure:** ebay-ato banner now links the real `reference/ebay-trade-first.md` (N1 exists since prompt 003).

## Verification

- **Editor sign-off:** Editor A — 24 files (22 md + 2 recreated SVGs): 23 sign-offs + 1 fix (api/overview.md /health response shape vs health.ts). Editor B — 16 files (root/working docs, rules, 5 edited SVGs, findings-report tables): 14 sign-offs + 2 fixes (memory SVG section enumerations). Zero open defects. Both editors spot-verified load-bearing claims against source (evidence lists in their reports); Editor B independently re-ran the task recount and the infra-secrets label-parity diff.
- **Build:** replicated deploy-docs.yml in a scratchpad mirror of the aifactory3.5 docs-site (copy docs → both sed rewrites → recursive img copy → `npm run build`, hardlinked node_modules, `onBrokenLinks: 'throw'` in force): **SUCCESS, exit 0, zero broken-link/broken-markdown-link warnings** — run twice, the second time after the last post-editor touch-ups. The only warnings are the pre-existing onBrokenMarkdownLinks deprecation and the pre-existing missing root `docsDir` (absent in the production site too).
- **Stale-marker sweep:** Etsy-as-live, "Claude Vision", client-side WASM/@imgly, 6-tab/More-tab/camera-FAB, JWT+bcrypt/refresh-token — **zero hits** outside historical records (ship-log, completed-task ledger rows now annotated with supersession notes, EXECUTED-stamped plans, research snapshots) and the audit/findings docs themselves. Sweep caught two leftovers that were fixed: "Claude Vision / Gemini" label in the ebay-trade-first-pipeline SVG pair (→ "Vision chain (Gemini / Claude)", parity kept) and scan-flow.md's "camera FAB" wording (→ "center Scan button").

**Files touched this pass:** 42 edited (31 by writers W1–W5, 8 by orchestrator, 3 editor fixes) + 4 git-rm'd SVG twins + 123 untracked duplicates/leftovers deleted (117 screenshots + 6 voice PNGs) + 90 PNGs moved to docs/verification-archive/ + this log.
