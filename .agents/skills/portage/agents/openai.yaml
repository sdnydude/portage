interface:
  display_name: "Portage"
  short_description: "Repo-specific patterns and workflows for portage"
  default_prompt: "Use the portage repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true