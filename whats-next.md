<original_task>
Session arc (2026-07-14 late → 07-15 early): started as "merge #223 + onboarding build" — discovered #223–#226 ALL merged last session (iPhone touch proof already passed; stale prompt). Pivoted per Stephen: (1) onboarding expansion planned + adversarially reviewed, then (2) INTERRUPTED by a bigger directive — fully-responsive UI (desktop/iPad/mobile polish) must ship BEFORE onboarding (tutorial screenshots must capture the new shell). Responsive Shell (Phase R0) is fully designed, HIG-aligned, spec'd, planned, mockup-approved-pending, and ready to BUILD in the next session.
</original_task>

<work_completed>
1. **Onboarding expansion — plan done, queued behind R0.** Spec branch pushed, PR #228 open (docs-only). Implementation plan `docs/superpowers/plans/2026-07-14-onboarding-expansion.md` (11 tasks, TDD, full code) written + adversarial architect/engineer review (both APPROVE-WITH-FIXES; all fixes applied: carousel height budget w-28, RSC test-pattern spike flag + orders/[id] fallback, 2 false premises corrected, dead class dropped, capture script reuses e2e/session-stub, Cache-Control minor → registry deferred). Artifact: https://claude.ai/code/artifact/5b77da94-594b-4de0-a583-7af1e4d8905c . NOTE: plan predates the R0 shell — after R0 ships, re-verify capture manifests/coords against the new chrome (5-tab bar, avatar header) before executing.
2. **Responsive UI program approved + phased** — docs/TODO.md "Responsive UI Program": R0 shell (now) → R1 master-detail workbench (comps live in detail pane) → R2 drag-drop ingest → R3 Porter dock + conversation history UI (endpoints exist unused: porter.ts:231,251) → R4 QR phone-camera handoff. Deferred: keyboard shortcuts, table views, dhg-app-shell template extraction (registry), Ollama-builder experiment (next ship, needs this ship's Claude-only baseline).
3. **R0 spec** `docs/superpowers/specs/2026-07-15-responsive-shell-design.md` — AppShell (root layout, admin passthrough), Sidebar 240/72 collapsible (main 5 + divider + Messages/Settings secondary + Admin role item), TopBar (title · AskPorterBar · unread/theme/avatar-menu), floating glass TabBar, breakpoints md/lg CSS-only, content-container width system, R1 pane + R3 dock slots reserved.
4. **HIG alignment package (Stephen-approved):** 5 tabs (More OUT of bar → PageHeader avatar w/ unread dot, renders BESIDE page actions — conflict found by sweep agent, resolved side-by-side); Scan FAB kept as DOCUMENTED DEVIATION; compact minimized bar replaces Home-chip (never-absent HIG model; permanent on non-tab routes, minimize-on-scroll on tab routes, reduced-motion = fade); sidebar carries more than the bar; accessory shelf RESERVED for future live-status (scan upload/publish progress/Porter stream); glass = chrome only (never content cards); 44px targets/12-24px spacing; prefers-reduced-transparency/contrast/motion as real render paths.
5. **HIG research** `docs/research/2026-07-15-apple-hig-ios26-shell-alignment.md` — full, gaps closed via Apple JSON data endpoints (`developer.apple.com/tutorials/data/design/human-interface-guidelines/<page>.json` — the HIG site is a JS SPA; plain fetches return scaffolding). Confirmed: no iOS margin numbers exist in HIG at all; iPad windowed-resize = behavior-based mandate (breakpoints are HIG-compatible technique, not prescription); sheets detents; search-bottom convention. Posted to registry insights. iOS NATIVE APP planned after R0 + onboarding — this doc is its design baseline.
6. **R0 implementation plan** `docs/superpowers/plans/2026-07-15-responsive-shell.md` — 11 tasks, full code, tdd rhythm. Dispatch order: 1 → 2 → 3(Fable) → 6 → 5 → 4 → 7 → 8 → 9 → 10 → 11(Fable). T8 = TabBar 5-tabs/compact/minimize-on-scroll/ownership-unification (AppShell renders bar on ALL non-admin routes; (tabs)/layout mount removed same commit).
7. **Multimodel build protocol (Labs project):** Sonnet 5 builds (effort low/medium per task table in plan), Fable orchestrates + builds T3/T11 + reviews every boundary, Opus 4.8 escalation after 2 failed reviews. Dispatch log live: `docs/labs/dispatch-log.jsonl` (6 entries, real usage: 3 reviews/research ~96–197k tokens each). Log every dispatch: ts/ship/task/model/effort/turn_type(code|test|wire|debug|review|verify|docs|research|capture)/tokens/tool_uses/duration/review/retries/notes.
8. **tdd one-test hard rule** `.claude/rules/tdd-one-test-per-write.md` (+ playbook memory header): ONE test per Write/Edit, no exceptions, include VERBATIM in every test-writing subagent prompt — Stephen directive, token-burn correction (posted).
9. **Mockups committed:** `docs/design/mockups/responsive-shell/` — 7 SVGs incl. v2 HIG updates + endstate R1/R3 (phase-labeled). Stephen loved the direction ("makes eBay look 2000-2005").
10. Branch `feat/responsive-shell` @ 7c26774: spec, plan, research, TODO phasing, tdd rule, dispatch log, mockups. Registry tonight: 3 deferred items, 1 correction, 1 insight, session report.
</work_completed>

<work_remaining>
1. **BUILD R0** — execute `docs/superpowers/plans/2026-07-15-responsive-shell.md` on `feat/responsive-shell` via subagent-driven development: Sonnet 5 per task (effort per plan's dispatch table), Fable builds Task 3 (AppShell) + Task 11 (DoD) inline, two-stage review each boundary, Opus escalation rule, log every dispatch to docs/labs/dispatch-log.jsonl. Mockup v2 approval from Stephen was pending at session end — confirm before Task 1.
2. **R0 DoD (Task 11):** typecheck/lint/685-api/408-web green + e2e + `docker compose up -d --build portage-app` + walk 390×844 / 820×1180 / 1440×900 light+dark + screenshot proof + PR (CodeRabbit required check; merge --merge).
3. **Then onboarding expansion build** (plan ready; re-verify capture manifests against new shell first) → then video v1 (brainstorm checklist in prior handoff/TODO) → R1–R4 phases as Stephen sequences.
4. Stale CLAUDE.md test counts (4 files, Stephen's uncommitted edits) — still outstanding.
</work_remaining>

<critical_context>
- **Sonnet build caveats from tonight's own reviews:** import inserters must respect "use client" first-line (past prod-break); PageHeader avatar+action coexist (flex gap-2) — never let action suppress avatar; AppShell breakpoint switching is CSS-ONLY (hidden lg:block / lg:hidden — no matchMedia in render path).
- **tdd-guard:** apps/web IS guarded; one-test-per-write HARD RULE; web tests via `npm run test -w apps/web -- <file>`; validator hedge → retry same edit verbatim once.
- **Deploy:** image-baked; `docker compose up -d --build portage-app`; no hot reload.
- **Git:** stage explicit paths only (Stephen's dirty CLAUDE.mds + untracked prompts/research/whats-next in tree); subjects ≤72 chars (hook enforces); no co-author trailers; direct push to main blocked; CodeRabbit required.
- **Apple HIG data endpoints:** `/tutorials/data/design/human-interface-guidelines/<page>.json` — reusable technique, in research doc §9.
- **Porter ?q= auto-send:** window.location + ref guard, NOT useSearchParams (Suspense requirement on client pages).
- **whats-next.md + prompts/ + research/ untracked at root are Stephen's** — do not commit or clean.
</critical_context>

<next_session_prompt>
1. Confirm mockups v2 approved (docs/design/mockups/responsive-shell/), then BUILD Phase R0: execute docs/superpowers/plans/2026-07-15-responsive-shell.md on feat/responsive-shell, subagent-driven — Sonnet 5 builders (effort per plan table), you build Tasks 3+11 inline, review every task boundary, Opus 4.8 after two failed reviews, one-test tdd rule verbatim in every builder prompt, log every dispatch to docs/labs/dispatch-log.jsonl.
2. Finish with Task 11 DoD: full gates + rebuild container + 3-viewport walk light/dark + screenshots + PR.
3. Queued after R0: onboarding expansion build (re-verify capture manifests against new shell), then video v1.
</next_session_prompt>
