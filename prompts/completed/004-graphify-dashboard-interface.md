<objective>
Finish the graphify knowledge-graph dashboard at :8018. The page currently promises interactivity it doesn't have — it says to drill down on rows for more info, but the rows are static text. Build the actual drill-down interface and reorganize how the knowledge is presented so the graph is explorable, not just listed.
</objective>

<context>
portage-graph is an nginx container serving graphify-out/ (the code knowledge graph generated by the graphify tool) at 10.0.0.251:8018; the Stack Portal lives at :8018/portal/ (PR #201). Read CLAUDE.md and the registry KB (10.0.0.251:8011, project_name "portage" — search "graphify", "portage-graph", PR #200/#201 notes) for how this was built before touching anything.
</context>

<team>
Model policy: orchestrator and any subagents run on Fable (claude-fable-5); dispatch Agent calls with model: "fable" explicitly.
</team>

<research>
MANDATORY before any edit — do not skip, do not assume:
1. Determine how graphify-out/ is produced: is the dashboard HTML generated fresh by `graphify update .` (meaning direct edits get overwritten on the next run)? Inspect the graphify skill/tool (~/.claude/skills/graphify/) and the generated output structure to answer this definitively.
2. Based on the answer, choose where the interface work must live so it SURVIVES regeneration: generator template, post-process step, or a separate layer that reads graphify's data files. State the finding and the chosen approach with evidence before building.
3. Inventory what data actually exists to drill into (nodes, edges, communities, god nodes, per-file detail — whatever graphify emits as JSON/data files). The interface can only present what the data supports; list what's available first.
If any of this cannot be determined from the code, tool source, and KB — stop and ask Stephen. Do not guess the regeneration behavior; a wrong guess destroys the work on the next `graphify update .`.
</research>

<requirements>
1. Drill-down: clicking a row/node opens its detail — the info the static text currently only alludes to (definition location, relationships, callers/callees or imports, community membership, whatever the data supports). No dead-end rows: every element that implies "more info" must deliver it or lose the affordance.
2. Knowledge presentation reorganized: group by meaningful structure (communities, layers, domains — driven by what graphify's data actually contains), with search/filter across nodes, and a sensible landing hierarchy (overview → area → node detail) instead of one flat static page.
3. Self-contained static site — nginx serves files; no backend. All interactivity client-side against graphify's emitted data files.
4. Match the existing dashboard's and portal's visual language; both light/dark if the current pages support it.
5. Zero-defect gate: no placeholder text, no fake data, no promised-but-missing interactions — this is exactly the defect being fixed; do not reintroduce it.
</requirements>

<constraints>
- portage-graph container: do not change nginx config or docker-compose without explicit approval — file changes inside the served directory (or the generator) only. If the chosen approach requires infra changes, stop and ask first.
- Remember the bind-mount inode rule: if the build wipes/recreates the served directory, the nginx container may serve a stale ghost — restart the consumer container after deploying and verify live.
</constraints>

<output>
- The interface work in whatever location the research phase proved survives regeneration (state the location and why in the log).
- `./docs/audits/2026-07-graphify-interface-log.md` — research findings (generation behavior, chosen approach, evidence), data inventory, what was built, how it survives `graphify update .`.
</output>

<verification>
Before declaring complete (Definition of Done — observe live behavior, not just files written):
- Open 10.0.0.251:8018 in a browser; click through overview → area → node detail on at least 5 different nodes; screenshot each level.
- Run `graphify update .`; confirm the interface survives; re-verify drill-down live afterward and screenshot proof.
- grep the served pages for the old "drill down on rows" static-text promise — the phrase must now be backed by working interaction or removed.
</verification>

<success_criteria>
Every row that offers drill-down delivers it live; knowledge is organized hierarchically with search; the interface provably survives graph regeneration; screenshots document all of it.
</success_criteria>
